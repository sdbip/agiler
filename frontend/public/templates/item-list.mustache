{{#items}}
<div class="{{type}} no-select {{#isNew}} hidden {{/isNew}}" onclick="toggle({element: this, event})">
  {{#canPromote}}
  <span class="promote-button" id="promote-{{id}}" onclick="promote({element: this, event})">✵</span>
  {{/canPromote}}
  {{#canComplete}}
  <input type="checkbox" id="{{id}}" onchange="toggle({element: this, event})"
      {{#isCompleted}} checked {{/isCompleted}}
    >
  <label for="{{id}}"></label>
  {{/canComplete}}
  <span>{{title}}</span>
</div>
{{/items}}
 